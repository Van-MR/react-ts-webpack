import React from 'react';

interface IEpisodes {
  name: string;
}

const List = ({ episodes }) => {
    return (
      <div>
        <p>this is list page</p>
      </div>
    )
}

export default List;
